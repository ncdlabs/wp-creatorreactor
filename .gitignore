# Build artifacts (see build-zip.sh)
creatorreactor-*.zip
wp-creatorreactor-*.zip

# Composer (if added later)
/vendor/

# Deploy (local paths / credentials; not for the repo)
deploy-hostinger.sh

# Environment & secrets
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Editors / IDEs
.idea/
.cursor/
*.swp
*.swo
*~

# Logs & caches
*.log
*.cache
.phpunit.result.cache
.php-cs-fixer.cache

# Node (if you add a JS toolchain later)
node_modules/
